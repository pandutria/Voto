<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VOTO" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/yelow</item>
        <item name="android:navigationBarColor">@color/yelow</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VOTO" parent="Base.Theme.VOTO" />
</resources>